[pytest]

# Enable CLI log capturing and set the log level to INFO
log_cli = true
log_cli_level = INFO

# Register your custom markers to avoid warnings
markers =
    titles: mark tests that check webpage titles
    num: mark tests that use the num marker
    media: mark tests that use the media marker
